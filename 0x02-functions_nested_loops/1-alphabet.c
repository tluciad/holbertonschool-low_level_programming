#include <main.h>
/**
*print_alphabet - alphabet in lowercase with _putchar
*Return: Always 0 (Succes)
*/
void print_alphabet(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
	_putchar(ch);
	};

}
