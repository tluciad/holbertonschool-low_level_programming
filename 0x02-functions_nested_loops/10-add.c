#include "main.h"

/**
* add - function that adds two integers and returns the result
* @t: first integer
* @d: second integer
* Return: addition of two integers
*/

int add(int t, int d)
{
	return (t + d);
}
